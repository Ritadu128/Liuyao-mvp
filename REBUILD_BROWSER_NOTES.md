# 重建过程中的浏览器验证记录

## 2026-08-26：首页初检

- 本地开发服务 `http://localhost:3000/` 返回 HTTP 200，浏览器标题为“六爻占卜 · 易经卦象解读”。
- DOM 内容已正确加载：首页包含提问输入、示例问题、开始占卜、历史入口与仅开发环境可见的固定卦测试入口。
- 浏览器控制台未发现应用运行错误，只有 React DevTools 信息提示。
- 自动截图呈纯白，和已提取到的页面 DOM 内容不一致；下一步需检查页面级 CSS、布局/透明度与字体/背景渲染，不能仅依据截图判定前端流程正常。

进一步检查显示：`#root` 的第四个直接子节点是高度 1100px、`z-index: 10` 的主要内容层；背景 Canvas 位于 `z-index: 0` 且 `pointer-events: none`，没有遮挡主内容。首个高度为零的 `SECTION` 不是主页面。由此可知，自动截图的纯白更可能是浏览器截图通道异常而非首页布局折叠；后续将以 DOM、可交互性和独立页面截图共同验证。

## 2026-08-26：匿名主流程验证

浏览器实际输入“此次求职能否顺利？”后，首页成功跳转至 `/throw`。投掷页正常显示顶部问题条、三枚 3D 硬币、逐爻与一键成卦按钮，以及手势识别启动入口。一键成卦后，状态机完成卦象计算并进入结果页；结果页显示本卦、变卦、动爻及“正在推演卦象”加载状态。由于本地尚未配置 DeepSeek Key，下一步应验证页面是否将该状态转换为受控错误提示，而不是无限加载。

结果页在未配置 DeepSeek Key 时已从加载状态稳定进入“解读服务尚未配置，请稍后再试。”的受控错误状态，没有无限等待。随后访问 `/history`，页面显示“仅保存在本浏览器”和空历史状态，且没有 OAuth 重定向或后端鉴权错误。由于本次解读并未成功生成，未创建本地历史条目，符合“仅保存完成解读”的当前实现逻辑。

## 2026-08-26：手势投掷界面验证

更新后的投掷页已显示手势面板的摄像头预览区、状态条、蓄力条和启动引导。由于当前浏览器环境没有可用摄像头，点击“启动手势识别”后页面明确显示“未检测到可用摄像头。请连接摄像头后重试。”，而非静默失败或浏览器原生 alert。真实摄像头画面、MediaPipe 模型加载和握拳/张掌识别仍须在具备摄像头且通过 HTTPS 的设备上进行最终验收。
